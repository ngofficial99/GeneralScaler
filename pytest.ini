[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests
    e2e: End-to-end tests
    slow: Slow running tests
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings
asyncio_mode = auto
